<div class="balloons-fields">
  <p class="text-right section-partial">Balão</p>
  <%= link_to_remove_association "<i class='fa fa-times-circle times-color' aria-hidden='true'></i><span class='plus-name'>Remover</span>".html_safe, f, data: { confirm: 'Tem certeza que deseja apagar este balão?'} %>
  <br>
  <div class="btn-group radio-options" data-toggle="buttons">
    <label class="btn btn-default btn-xs active text-option">
      <%= f.radio_button("type", "Texto", checked: "checked") %> Texto
    </label>
    <label class="btn btn-default btn-xs img-option">
      <%= f.radio_button("type", "Imagem/GIF") %> Imagem/GIF
    </label>
  </div>
  <br>
  <div class="balloon-form input-text-balloon">
    <%= f.input :content, label: false, placeholder: "Texto", input_html: { data: { emojiable: true }, class: "emoji-picker-container balloon-form-input-text", maxlength: 260 } %>
    <%= f.input :link, label: false, placeholder: "Link para texto", input_html: { class: "balloon-form-input balloon-form-link" } %>
  </div>

  <div class="balloon-form input-img-balloon hidden">
    <%= f.input :balloon_img, label: false %>
    <%= f.input :balloon_img_cache, as: :hidden %>
    <%= f.input :remote_balloon_img_url, label: false, placeholder: "Link da imagem ou do GIF", input_html: { class: "balloon-form-input balloon-form-link" } %>
  </div>

</div>

<div class="balloon-form weather">
  <%= f.input :weather, placeholder: "Digite o nome da cidade" %>
  <p>Obs.: Use esse campo para enviar a previsão do tempo de alguma cidade.</p>
  <p>Fornecemos o valor da temperatura (mín e máx), velocidade do vento e umidade do ar.</p>
  <a href="#" id="weather" class="btn btn-primary">Pesquisar</a>
</div>
  <div class="balloon-form input-text-balloon">
    <%= f.input :address, label: false, placeholder: "Endereço" %>
  </div>
